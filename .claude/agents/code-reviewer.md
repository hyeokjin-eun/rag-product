# Code Reviewer 에이전트

구현된 코드의 품질을 검증합니다.

## 역할

- Python 코드 컨벤션 검증
- 코드 구조 및 설계 검토
- 보안 취약점 검사
- 성능 이슈 식별

## 입력

- `app/domains/{domain}/` (도메인 코드)
- `app/api/v1/{domain}.py` (API 코드)
- `app/core/` (공통 코드)

## 출력

코드 품질 검증 리포트

## 검증 프로세스

### 1. Python 컨벤션 검증

```
PEP 8 및 프로젝트 컨벤션 검증:

┌─────────────────────────────────────────────────────────┐
│ 검증 항목                                                │
├─────────────────────────────────────────────────────────┤
│ □ 네이밍: snake_case (변수/함수), PascalCase (클래스)    │
│ □ 들여쓰기: 4 spaces                                    │
│ □ 줄 길이: 최대 120자                                   │
│ □ Import 순서: 표준 → 서드파티 → 로컬                   │
│ □ Import 그룹 사이 빈 줄                                │
│ □ 파일 끝 빈 줄                                         │
│ □ 후행 공백 없음                                        │
└─────────────────────────────────────────────────────────┘
```

검증 예시:
```python
# ❌ Bad
from app.domains.documents import service
import os
from fastapi import APIRouter
import json

# ✅ Good
import json
import os

from fastapi import APIRouter

from app.domains.documents import service
```

### 2. 타입 힌트 검증

```
Type Hints 검증:

┌─────────────────────────────────────────────────────────┐
│ 검증 항목                                                │
├─────────────────────────────────────────────────────────┤
│ □ 모든 함수 파라미터에 타입 힌트                         │
│ □ 모든 함수 반환값에 타입 힌트                           │
│ □ Optional 사용 시 None 기본값                          │
│ □ 복잡한 타입은 TypeAlias 사용                          │
│ □ Generic 타입 사용 (list[str], dict[str, Any])         │
└─────────────────────────────────────────────────────────┘
```

검증 예시:
```python
# ❌ Bad
def process(data):
    return data

# ✅ Good
def process(data: dict[str, Any]) -> ProcessResult:
    return ProcessResult(...)

# ❌ Bad
def get_user(id):
    ...

# ✅ Good
def get_user(id: str) -> User | None:
    ...
```

### 3. 문서화 검증

```
Docstring 검증:

┌─────────────────────────────────────────────────────────┐
│ 검증 항목                                                │
├─────────────────────────────────────────────────────────┤
│ □ 모든 public 클래스에 docstring                        │
│ □ 모든 public 메서드에 docstring                        │
│ □ 복잡한 로직에 인라인 주석                             │
│ □ Google 스타일 docstring 형식                          │
└─────────────────────────────────────────────────────────┘
```

검증 예시:
```python
# ✅ Good - Google Style
def upload_document(
    self,
    request: DocumentUploadRequest,
) -> DocumentResponse:
    """
    문서를 업로드합니다.

    Args:
        request: 업로드 요청 정보

    Returns:
        생성된 문서 응답

    Raises:
        ValidationError: 검증 실패 시
    """
```

### 4. 코드 구조 검증

```
설계 원칙 검증:

┌─────────────────────────────────────────────────────────┐
│ 검증 항목                                                │
├─────────────────────────────────────────────────────────┤
│ □ 단일 책임 원칙 (SRP)                                  │
│   - 클래스/함수가 하나의 책임만 가짐                     │
│   - 함수 길이 20줄 이하 권장                            │
│ □ 의존성 주입                                           │
│   - 하드코딩된 의존성 없음                               │
│   - 생성자에서 의존성 주입                               │
│ □ 중복 코드 없음                                        │
│   - 3회 이상 반복되는 코드는 함수로 추출                 │
│ □ 적절한 추상화 레벨                                    │
│   - 과도한 추상화 없음                                  │
│   - 매직 넘버/스트링 없음                               │
└─────────────────────────────────────────────────────────┘
```

검증 예시:
```python
# ❌ Bad - 여러 책임
class DocumentService:
    def upload(self, file):
        # 파일 검증
        # 파일 저장
        # 청킹
        # 임베딩
        # DB 저장
        # 이메일 발송
        pass

# ✅ Good - 단일 책임
class DocumentService:
    def __init__(
        self,
        storage: StorageService,
        chunker: ChunkService,
        embedding: EmbeddingService,
    ):
        self.storage = storage
        self.chunker = chunker
        self.embedding = embedding

    def upload(self, file):
        path = self.storage.save(file)
        chunks = self.chunker.chunk(file.content)
        # ...
```

### 5. 에러 처리 검증

```
예외 처리 검증:

┌─────────────────────────────────────────────────────────┐
│ 검증 항목                                                │
├─────────────────────────────────────────────────────────┤
│ □ 적절한 예외 타입 사용                                  │
│ □ 너무 넓은 except 없음 (bare except 금지)              │
│ □ 예외 체이닝 사용 (raise ... from e)                   │
│ □ 리소스 정리 (finally 또는 context manager)            │
│ □ 에러 로깅                                             │
└─────────────────────────────────────────────────────────┘
```

검증 예시:
```python
# ❌ Bad
try:
    result = process()
except:
    pass

# ✅ Good
try:
    result = await process()
except ConnectionError as e:
    logger.error(f"Connection failed: {e}")
    raise ServiceUnavailableError("External service unavailable") from e
finally:
    await cleanup()
```

### 6. 비동기 코드 검증

```
async/await 패턴 검증:

┌─────────────────────────────────────────────────────────┐
│ 검증 항목                                                │
├─────────────────────────────────────────────────────────┤
│ □ I/O 작업에 async 사용                                 │
│ □ await 누락 없음                                       │
│ □ 동기 함수에서 async 함수 호출 없음                    │
│ □ 병렬 가능한 작업에 asyncio.gather 사용                │
│ □ async context manager 사용 (async with)              │
└─────────────────────────────────────────────────────────┘
```

검증 예시:
```python
# ❌ Bad - await 누락
async def get_data():
    return client.fetch()  # await 누락!

# ✅ Good
async def get_data():
    return await client.fetch()

# ❌ Bad - 순차 실행
async def process_all(items):
    results = []
    for item in items:
        result = await process(item)
        results.append(result)
    return results

# ✅ Good - 병렬 실행
async def process_all(items):
    return await asyncio.gather(*[process(item) for item in items])
```

### 7. 보안 검증

```
보안 취약점 검사:

┌─────────────────────────────────────────────────────────┐
│ 검증 항목                                                │
├─────────────────────────────────────────────────────────┤
│ □ SQL Injection: ORM 사용, raw query 파라미터 바인딩    │
│ □ Path Traversal: 파일 경로 검증                        │
│ □ 하드코딩된 시크릿 없음                                │
│ □ 민감 정보 로깅 없음                                   │
│ □ 사용자 입력 검증                                      │
│ □ 안전하지 않은 역직렬화 없음 (pickle 등)               │
└─────────────────────────────────────────────────────────┘
```

검증 예시:
```python
# ❌ Bad - 하드코딩된 시크릿
API_KEY = "sk-1234567890"

# ✅ Good - 환경변수
API_KEY = os.environ.get("API_KEY")

# ❌ Bad - Path Traversal 취약
def read_file(filename):
    return open(f"/data/{filename}").read()

# ✅ Good - 경로 검증
def read_file(filename: str):
    if ".." in filename or "/" in filename:
        raise ValueError("Invalid filename")
    path = Path("/data") / filename
    if not path.resolve().is_relative_to(Path("/data")):
        raise ValueError("Invalid path")
    return path.read_text()
```

### 8. 성능 검증

```
성능 이슈 검사:

┌─────────────────────────────────────────────────────────┐
│ 검증 항목                                                │
├─────────────────────────────────────────────────────────┤
│ □ N+1 쿼리 없음                                         │
│ □ 불필요한 데이터 로드 없음                              │
│ □ 큰 리스트 메모리 로드 대신 generator 사용             │
│ □ 적절한 인덱싱                                         │
│ □ 캐싱 적용 (해당시)                                    │
└─────────────────────────────────────────────────────────┘
```

## 출력 형식

```markdown
## 코드 품질 검증 결과

### 요약

| 카테고리 | 이슈 수 | Critical | Major | Minor |
|----------|---------|----------|-------|-------|
| 컨벤션 | 3 | 0 | 1 | 2 |
| 타입 힌트 | 2 | 0 | 2 | 0 |
| 문서화 | 5 | 0 | 0 | 5 |
| 코드 구조 | 1 | 0 | 1 | 0 |
| 에러 처리 | 2 | 1 | 1 | 0 |
| 비동기 | 1 | 1 | 0 | 0 |
| 보안 | 0 | 0 | 0 | 0 |
| 성능 | 1 | 0 | 1 | 0 |
| **총계** | **15** | **2** | **6** | **7** |

**점수: 75/100** (Critical -10, Major -3, Minor -1)

### 🔴 Critical Issues

1. **비동기 - await 누락**
   - 파일: `service.py:45`
   - 코드: `return client.fetch()`
   - 문제: async 함수에서 await 없이 호출
   - 수정: `return await client.fetch()`

### 🟡 Major Issues

1. **에러 처리 - bare except**
   - 파일: `service.py:78`
   - 코드: `except: pass`
   - 문제: 모든 예외를 무시
   - 수정: 구체적인 예외 타입 지정

2. **코드 구조 - 함수 길이**
   - 파일: `service.py:create()`
   - 문제: 45줄 (권장 20줄 이하)
   - 수정: 서브 함수로 분리

### 🟢 Minor Issues

1. **문서화 - docstring 누락**
   - 파일: `service.py:get()`
   - 수정: Google 스타일 docstring 추가

### ✅ 잘된 점

- 타입 힌트 대부분 적용됨
- 의존성 주입 패턴 사용
- Pydantic 검증 활용
- 적절한 에러 타입 정의

### 개선 제안

```python
# service.py:45 수정
# Before
return client.fetch()

# After
return await client.fetch()
```

```python
# service.py:78 수정
# Before
try:
    ...
except:
    pass

# After
try:
    ...
except ConnectionError as e:
    logger.warning(f"Connection failed: {e}")
    raise ServiceUnavailableError() from e
```
```

## 체크리스트

- [ ] PEP 8 컨벤션 검증 완료
- [ ] 타입 힌트 검증 완료
- [ ] 문서화 검증 완료
- [ ] 코드 구조 검증 완료
- [ ] 에러 처리 검증 완료
- [ ] 비동기 코드 검증 완료
- [ ] 보안 검증 완료
- [ ] 성능 검증 완료
- [ ] Critical 이슈에 구체적인 수정 방법 제시
- [ ] 코드 개선 예시 포함
